---
import { ArrowRight, Phone, Zap, Shield, Award } from 'lucide-astro';
---

<section id="hero" class="relative min-h-[88vh] flex items-center justify-center overflow-hidden py-24">
  <!-- Background -->
  <div class="absolute inset-0 bg-gradient-to-br from-primary-600 via-primary-700 to-secondary-600"></div>
  <div class="absolute inset-0 bg-black/20"></div>
  
  <!-- Background Pattern -->
  <div class="absolute inset-0 opacity-10">
    <div class="absolute inset-0" style="background-image: url('data:image/svg+xml,%3Csvg%20width%3D%2260%22%20height%3D%2260%22%20viewBox%3D%220%200%2060%2060%22%20xmlns%3D%22http%3A//www.w3.org/2000/svg%22%3E%3Cg%20fill%3D%22none%22%20fill-rule%3D%22evenodd%22%3E%3Cg%20fill%3D%22%23ffffff%22%20fill-opacity%3D%220.1%22%3E%3Ccircle%20cx%3D%2230%22%20cy%3D%2230%22%20r%3D%224%22/%3E%3C/g%3E%3C/g%3E%3C/svg%3E'); background-repeat: repeat;"></div>
  </div>

  <div class="relative z-10 container-custom text-center text-white">
    <div class="animate-on-scroll">
      <!-- Badge -->
      <div class="inline-flex items-center space-x-1 glass rounded-full px-4 py-2 mb-6">
        <Award class="w-5 h-5 text-accent-400" />
        <span class="text-sm font-medium">+45 años de experiencia</span>
      </div>

      <!-- Main Heading -->
      <h1 class="text-5xl md:text-5xl lg:text-6xl font-bold mb-6 leading-tight text-balance block">
        <span>Expertos en <span class="bg-gradient-to-br from-accent-400 to-yellow-400 bg-clip-text text-transparent">aire
        acondicionado</span> <span class="text-white">en Sevilla</span></span>
      </h1>

      <!-- Subtitle -->
      <p class="text-lg md:text-1xl text-gray-200 mb-8 max-w-3xl mx-auto leading-relaxed text-balance">
        Venta, instalación y mantenimiento de sistemas de aire acondicionado. 
        Soluciones profesionales para tu hogar y empresa.
      </p>

      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row justify-center mb-12 gap-10 items-center">
        <a href="/contacto" class="btn-primary text-lg px-8 py-4 group animate-slide-up transition-all duration-200 shadow-lg hover:shadow-xl" style="animation-delay: 0.4s;">
          Solicitar presupuesto
          <ArrowRight class="ml-2 w-5 h-5 transition-transform group-hover:translate-x-1" />
        </a>
        <a href="tel:954647966" class="nav-link text-lg px-8 rounded-full bg-transparent border-none text-white hover:bg-transparent focus:outline-offset-0 focus:outline-none focus:ring-offset-0 focus:ring-0 shadow-none gap-1 py-0 hover:shadow-none">
          <Phone class="w-5 h-5" />
          Llamar ahora
        </a>
      </div>

      <!-- Features -->
      <svg aria-hidden="true" focusable="false" style="position: absolute; width: 0; height: 0; overflow: visible;">
        <defs>
          <linearGradient id="g-accent-yellow" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" stop-color="#facc15" />   <!-- from-accent-400 -->
            <stop offset="100%" stop-color="#fb923c" /> <!-- yellow-400 -->
          </linearGradient>
        </defs>
      </svg>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-4xl mx-auto">
        <div class="glass rounded-lg p-6 text-center">
          <!-- Para iconos que usan stroke (lucide por defecto) -->
          <Zap class="w-8 h-8 mx-auto mb-3" stroke="url(#g-accent-yellow)" fill="none" />
          <h3 class="font-semibold mb-2">Agilidad</h3>
          <p class="text-sm text-gray-200">Instalamos tu equipo en unas pocas horas</p>
        </div>

        <div class="glass rounded-lg p-6 text-center">
          <!-- si quieres forzar también fill (no hace daño) -->
          <Shield class="w-8 h-8 mx-auto mb-3" stroke="url(#g-accent-yellow)" />
          <h3 class="font-semibold mb-2">Garantía</h3>
          <p class="text-sm text-gray-200">3 años de garantía en todos nuestros servicios</p>
        </div>

        <div class="glass rounded-lg p-6 text-center">
          <!-- si el icono está pensando para fill, usa fill -->
          <Award class="w-8 h-8 mx-auto mb-3" stroke="url(#g-accent-yellow)" />
          <h3 class="font-semibold mb-2">Certificado</h3>
          <p class="text-sm text-gray-200">Personal altamente cualificado y certificado</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 text-white">
    <div class="animate-bounce">
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
      </svg>
    </div>
  </div>
</section>

<style>
.nav-link {
  @apply hover:text-white flex items-center gap-2 px-1 py-2 transition-colors relative;
}
.nav-link:last-child {
  height: max-content !important;
}

.nav-link::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  width: 0;
  height: 2px;
  background: theme('colors.white');
  transition: all 0.3s ease;
  transform: translateX(-50%);
}

.nav-link:hover::after {
  width: 100%;
}
</style>